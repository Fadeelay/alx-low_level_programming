#include <stdio.h>
/**
 * main - Print numbers in hexadecimal
 * Return: Always 0
 */
int main(void)
{
	int num;
	char la;

	la = 'a';
	while
		(num < 10) {
			putchar(num + '0');
			num++;
		}
	while
		(la <= 'f') {
			putchar(la);
			la++;
		}
	return (0);
}
